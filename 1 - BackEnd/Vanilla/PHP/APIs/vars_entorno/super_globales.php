<?php
	/* ##########===========================########## */
	/* ######===--- Super arrays globales ---===###### */
	/* ##########===========================########## */

	/* Se encuentran presentes en todos los documentos PHP.

	$GLOBALS, el array que se encarga de almacenar variables de forma global.
	$_SERVER, el array que se encarga del servidor.
	$_POST, el array que se encarga de los elementos enviados de un formulario por medio del metodo (POST).
	$_GET, el array que se encarga de los elementos enviados de un formulario por medio del metodo (GET).
	$_REQUEST, el array que se encarga de los elementos enviados de un formulario por medio del metodo (POST y GET).
	$_SESSION, el array que se encarga de los usuarios los cuales se encuentran en sesion.
	$_COOKIE, el array que se encarga de almacenar todas las cookies del servidor.
	$_FILES, el array que se encarga de cargar archivos a la pagina. */
?>