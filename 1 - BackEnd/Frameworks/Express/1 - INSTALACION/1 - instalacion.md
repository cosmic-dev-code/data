### ========================================= ###
###### ===--- Instalacion por (npm) ---=== ######
### ========================================= ###

Instalamos via (npm).

```bat
	npm install express -E
```

Ahora podemos extraer Express.

```js
	const express = require("express");
```