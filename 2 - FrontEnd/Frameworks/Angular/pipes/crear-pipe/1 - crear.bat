
: Crea un pipe llamado (reverse) en la ruta [src/app/].
ng g p reverse

: Crea un pipe llamado (reverse) dentro de una carpeta llamada (pipes) en la ruta [src/app/pipes/].
ng g p pipes/image

: Los archivos que se crean son: 
	: --- (reverse.pipe.spec.ts) 
	: --- (reverse.pipe.ts)
		: --- Este es el archivo que lleva la logica del pipe.